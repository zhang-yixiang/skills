interface:
  display_name: "Claude Handoff"
  short_description: "Hand off to a background agent"
  default_prompt: "Use $claude-handoff to hand this conversation off to a fresh background agent."
policy:
  allow_implicit_invocation: false
